// Japan English. UI strings mirror the base English dictionary.
// Product catalogue is filtered to Japanese products by getProductsForLocale().
import en from "./en";

const enJp = en;

export default enJp;
